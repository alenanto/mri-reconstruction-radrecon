_target_: src.data.dce_datamodule.DCEDataModule

batch_size: 1 # Only batch size 1 is supported currently
train_val_test_split: [0.8, 0.1, 0.1]
num_workers: 0
pin_memory: False

# Parameter for radially undersampling
time_frames: 1
spokes_per_frame: 288

est_sens_maps: True
ssdu: False
